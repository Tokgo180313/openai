/** 模型能力类型，与 ai_models.model_type 对齐 */
export enum ModelType {
  TEXT = 'text',
  IMAGE = 'image',
  VISION = 'vision',
  REASONING = 'reasoning',
  MULTIMODAL = 'multimodal',
  AUDIO = 'audio',
  EMBEDDING = 'embedding',
}
